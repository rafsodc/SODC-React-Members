const event = {
  EVENTS: '/events',
  FUTURE_EVENTS: '/events?future=true',
  POST: '/events'
}

const ticketType = {
  POST: '/ticket_types'
}

export default {
  event: event,
  ticketType: ticketType
}